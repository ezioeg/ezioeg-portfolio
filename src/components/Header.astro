---

---

<header class="header">
    <nav class="nav">
        <ul class="nav-list">
            <li><a href="#experiencia" class="nav-link">Experiencia</a></li>
            <li><a href="#proyectos" class="nav-link">Proyectos</a></li>
            <li><a href="#sobre-mi" class="nav-link">Sobre mí</a></li>
            <li>
                <a href="mailto:ezioeg@gmail.com" class="nav-link">Contacto</a>
            </li>
        </ul>
    </nav>
</header>

<style>
    .header {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        z-index: 100;
        padding: 1rem 2rem;
        /* backdrop-filter: blur(10px); */
        /* background: rgba(255, 255, 255, 0); */
    }

    .nav {
        max-width: 1200px;
        margin: 0 auto;
        display: flex;
        justify-content: center;
    }

    .nav-list {
        display: flex;
        list-style: none;
        margin: 0;
        padding: 0;
        /* gap: 0.5rem; */
        background: transparent;
        padding: 0.5rem 0rem;
        border-radius: 50px;
        backdrop-filter: blur(10px);
        box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
    }

    .nav-link {
        text-decoration: none;
        color: #374151;
        font-weight: 500;
        padding: 0.5rem 1rem;
        border-radius: 25px;
        transition: all 0.3s ease;
    }

    .nav-link:hover {
        background: #6366f1;
        color: #a3e635;
    }

    @media (max-width: 768px) {
        .header {
            padding: 1rem;
        }

        .nav-list {
            gap: 1rem;
            padding: 0.5rem 1rem;
        }

        .nav-link {
            padding: 0.25rem 0.75rem;
            font-size: 0.9rem;
        }
    }
</style>
